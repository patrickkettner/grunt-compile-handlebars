'use strict';

var grunt = require('grunt');

exports.clean = {
  dev: function(test) {
      test.expect(1);


      test.done();
    },
  prod: function(test) {
      test.expect(1);


      test.done();
    }
};
